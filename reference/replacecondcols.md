# returns either a set of default colours, or if condition names are also colours, returns those

returns either a set of default colours, or if condition names are also
colours, returns those

## Usage

``` r
replacecondcols(condlevels)
```

## Arguments

- condlevels:

  the levels of a factor in a condition column
